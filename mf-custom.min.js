( function( window, $, undefined ) {

	$(document).ready(function() {
		
		$('#mf-custom-form').css('visibility', 'visible');
  
	  if ($("#element_2").val() == 1) {
	  	//$('#li_buttons').hide();
		$('#li_buttons').css('visibility', 'hidden');
	  }

	    $("#element_2").change(function() {
	      if ( $(this).val() > 1) {
	          //$('#li_buttons').show();
		      $('#li_buttons').css('visibility', 'visible');
	      }
	      else {
	        //$('#li_buttons').hide();
		    $('#li_buttons').css('visibility', 'hidden');
	      }
	  });
	});

})( this, jQuery );
